<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{ renderData.title or title or metadata.title }}</title>
        <meta name="Description" content="{{ renderData.description or description or metadata.description }}" />
        <link rel="stylesheet" href="{{ '/styles/main.css' | url }}" />
        <link
            href="https://fonts.googleapis.com/css?family=Poppins:200,300,400,500,600,700|Merriweather:300;400;700&display=swap"
            rel="stylesheet"
        />
        <link
            rel="alternate"
            href="{{ metadata.feed.path | url }}"
            type="application/atom+xml"
            title="{{ metadata.title }}"
        />
    </head>
    <body>
        <header>
            <nav>
                <h1 class="home"><img src="/img/avatar.svg" alt="Avatar images" /> <a href="{{ '/' | url }}">{{ metadata.title }}</a></h1>
            </nav>
        </header>
        {{ content | safe }}
    </body>
</html>
